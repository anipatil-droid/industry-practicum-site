# BMGT407 course site

Open `index.html` in a browser, or use the Live Server extension in VS Code.

To publish with GitHub Pages, create a repository, upload the contents of this folder to its root, then open **Settings > Pages** and deploy from the `main` branch and `/ (root)` folder.

Before submitting, replace the course details and contact information if your instructor asks for different content.
